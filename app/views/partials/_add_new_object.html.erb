<!-- Add a button for new instances of object -->
  <%= link_to(eval("new_#{model_name}_path"), class: "waves-effect waves-pink btn") do %>
    <strong>Add a New <%= model_name == "medium" ? "Media" : model_name.humanize %></strong>
  <% end %>
